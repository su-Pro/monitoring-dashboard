import Plan from './Plan';

const PlanAppConfig = {
  settings: {
    layout: {
      config: {},
    },
  },
  routes: [
    {
      path: 'plan',
      element: <Plan />,
    },
  ],
};

export default PlanAppConfig;
